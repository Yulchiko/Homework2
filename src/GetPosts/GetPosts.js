const GetPosts  = ({address}) => {

    return(
        <div>
            {address.street} - {address.suite} - {address.city} - {address.zipcode}
                    </div>
    );

}

export default GetPosts;